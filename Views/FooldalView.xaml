<UserControl x:Class="GnuConvert.Views.FooldalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            Height="Auto" Width="Auto">

    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="#FFF5F3EE" Opacity="1"/>
        </Grid.Background>
        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center">
            <TextBlock Text="Üdvözlünk a Gnu Convert programban!" FontSize="26" FontWeight="Bold" Margin="0,75,0,20" Foreground="#FF4D4335" FontFamily="Century Schoolbook" />
            <TextBlock Text="Itt láthatod a legfontosabb statisztikákat és az előfizetetett csomaghoz tartozó inforációkat." 
                   FontSize="16" TextAlignment="Center" Margin="0,0,0,30" FontFamily="Century Schoolbook" Foreground="#FF4D4335" />
            <TextBlock Text=""/>
        </StackPanel>
    </Grid>
</UserControl>

